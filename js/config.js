// ============================================
// SET YOUR PASSWORD HERE (before deploying)
// ============================================
window.SITE_CONFIG = {
  // Change this to the password she should type:
  password: "paula2026",

  // true = password must match exactly (A ≠ a)
  caseSensitive: true,
};

// --------------------------------------------
// GitHub Pages (free hosting) — quick steps:
// 1. Create a new GitHub repo (public).
// 2. Upload ALL files from this folder:
//      index.html, gutschein.pdf, css/, js/, assets/
// 3. Repo Settings → Pages → Deploy from branch
//      Branch: main (or master), folder: / (root)
// 4. Open the Pages URL, test password + PDF.
// 5. Put that URL into your QR code later.
//
// Note: This password check is client-side only
// (enough for a private gift, not bank security).
// --------------------------------------------
